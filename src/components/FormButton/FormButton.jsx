import React from "react";
import './FormButton.scss';

function FormButton() {
    return (
        <button className="FormButton">
            Сохранить
        </button>
    );
}

export default FormButton;